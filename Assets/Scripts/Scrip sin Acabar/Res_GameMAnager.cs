using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Res_GameMAnager : MonoBehaviour
{
    // Start is called before the first frame update
    /*void Start()
    {
        var MovimientoLobo = FindObjectOfType<MovimientoLobo>();

        this.OnProtagonistCheckpoint(MovimientoLobo);
        this.protagonistStats = sanchoStats;

    }

    public void OnProtagonistDeath(GameObject MovimientoLobo){

        this.protagonistStats.gameObject.SetActive(false);

        Invoke("Respawn", 1f);
    }

    void Respawn(){

        this.protagonistStats.gameObject.SetActive(true);
        this.protagonistStats.transform.position = loboStoredData.checkPointposition;

        this.protagonistStats.RestoreVidaStats(loboStoredData.vida);

    }

    public void OnProtagonististCheckpoint(MovimientoLobo loboStats){

        loboStoredData = new loboStats(MovimientoLobo.vida,MovimientoLobo.transform.position);
    }*/

}
